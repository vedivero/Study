<Call Super Constructor>

·super(매개값, ...)은 매개값과 동일한 타입,개수,순서가 맞는 부모 생성자를 호출한다.
·만약 부모 생성자가 없다면 컴파일 오류가 발생한다
·반드시 자식 생성자의 첫 줄에 위치해야 한다.
·부모 클래스에 기본 생성자가 없다면 필수적으로 작성해야 한다.
	→설명
	
	//자식 생성자가 생성되면
	자식 클래스(){
		//컴파일러는 부모생성자를 호출한다.
		super();  // ←부모 생성자의 매개 변수가 없는 생성자인데,
		
		//만약 부모 클래스가 매개변수가 있는 생성자일 경우
		//ex 부모클래스(int x){ }
		
		//부모 생성자의 매개변수와 동일하게 매개값을 주고 명시적으로 super를 호출해야 한다.
		//ex super(10);
	}
	
	